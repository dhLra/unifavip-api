import { Request, Response } from "express"
import { Service } from "./auth.service";

class authController {
    async authenticator(req: Request, res: Response) {    }
}

export default authController;